function sortNumberNew(num1, num2, num3) {
    let currRow = 0
    if (num1 > num2 && num1 > num3) {
        currRow = num1
    } else if (num2 > 1 && num2 > num3) {
        currRow = num2
    } else if (num3 > num1 & num3 > num2) {
        currRow = num3
    }
    console.log(currRow);
    for (let i = 1; i < sortNumberNew.length; i++) {

        if (num1 < currRow&&) {

        }

    }
} sortNumberNew(2, 1, 3)