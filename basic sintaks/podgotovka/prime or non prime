int n = Integer.parseInt(scanner.nextLine());

for(int i = 2; i <= n; i ++){
  boolean isPrime = true;
    for(int z = 2; z <= Math.sqrt(i); z++){
 if(i % z == 0){
 isPrime = false;
}

}
return isPrime;

}

Защо "z" е (z <= Math.sqrt(i)) по - малко от корен квадратен от "i" , а не просто "z" <= "i". Каква е функцията на Math.sqrt тук?
И това "i % z == 0" немога да разбера как определя нали едно число е просто?!?
Здравей, Math.sqrt() връща корена от числото в скобите. А целта е да се обиколят всички числа и да се види дали 
дадеото число се дели на тях с точност. Простите числа се делят само на едно и на себе си сточност, затова и
 цикъла почва от две. Променливата i върви по цислата от които искаме да намерим простите. А z обикаля по
 числата на които проверяваме дали i се дели с точност. z е до корен от i защото от корен от i до i няма
числа, които да се дели i с точност.